example-lambda:
  handler: functions/health-check/handler.handle
  vpc: ${self:custom.vpc}
  environment:
    POSTGRES_URL: ${env:POSTGRES_URL}
  events:
    - http:
        path: /health-check
        method: GET
        private: true
        cors: true
