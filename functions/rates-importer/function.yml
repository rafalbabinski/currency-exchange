rates-importer:
  handler: functions/rates-importer/handler.handle
  vpc: ${self:custom.vpc}
  environment:
    POSTGRES_URL: ${env:POSTGRES_URL}
  events:
    - http:
        path: /rates
        method: GET
        private: true
        cors: true
